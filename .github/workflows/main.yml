name: Deploy Produção

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            cd /var/www/projeto-conta-azul
            git pull origin master
            docker exec conta_azul_app composer install --no-dev --optimize-autoloader
            docker exec conta_azul_app php artisan migrate --force
            docker exec conta_azul_app php artisan config:cache
            docker exec conta_azul_app php artisan route:cache
            docker exec conta_azul_app php artisan view:cache
            docker exec conta_azul_app php artisan queue:restart
